{
  "name": "jsan-dev-ai",
  "version": "5.0.0",
  "private": true,
  "scripts": {
    "build:frontend": "npm --prefix frontend install && npm --prefix frontend run build",
    "check:backend": "npm --prefix backend install && npm --prefix backend run check",
    "db:init": "node database/src/cli.js init",
    "db:status": "node database/src/cli.js status",
    "db:verify": "node database/src/verify.js",
    "db:reset": "node database/src/cli.js reset"
  }
}
