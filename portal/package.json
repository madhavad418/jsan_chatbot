{
  "name": "jsan-dev-ai",
  "version": "5.0.0",
  "private": true,
  "scripts": {
    "build:frontend": "npm --prefix frontend install && npm --prefix frontend run build",
    "check:backend": "npm --prefix backend install && npm --prefix backend run check",
    "db:init": "npm --prefix database run init",
    "db:status": "npm --prefix database run status",
    "db:verify": "npm --prefix database run verify",
    "db:reset": "npm --prefix database run reset"
  }
}
